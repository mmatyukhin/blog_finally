<% @categories.each do |category| %>
  <%= link_to "show_category/#{category.id}", controller:"articles", action:"show_category", id: category.id do %>
  <div class="container-fluid"><%= category.category %></div>
  <% end %>
<% end %>
