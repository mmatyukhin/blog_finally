require 'rails_helper'

RSpec.describe CommentsController, type: :controller do
  # let (:comment) { 'This is test comment for rspec i dont know what to write here, so i will type something' }
  # let (:username) { subject.current_user }

  # before { binding.pry }

  # it { '' }

    # describe '#new' do
    # subject { get :new }
    #
    #   context 'when user has role consumer' do
    #   it { expect { subject }.to }
    #   end
    # end
    # describe '#create_new_comment' do
    # let(:instance) { described_class.new(comment,username) }
    # subject { instance.create }
    #
    # context 'when user is logged in' do
    #     let (:comment) { 'This is test comment for rspec i dont know what to write here, so i will type something' }
    #     let (:username) { subject.current_user }
    #   it "creating new comment" do
    #     expect(subject).to eq
    #   end
    # end
    #
    # context 'when user is logged out' do
    #
    #
    #   it "creating new comment" do
    #     expect{subject}.to
    #   end
    # end
  # end
end
